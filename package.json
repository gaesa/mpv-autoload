{
  "dependencies": {
    "core-js": "^3.43.0"
  },
  "scripts": {
    "build:webpack": "webpack",
    "build": "pnpm run build:webpack",
    "test": "deno test --vendor=false",
    "analyze": "if [[ ! -f ./dist/stats.json ]]; then pnpm run build; fi && webpack-bundle-analyzer ./dist/stats.json"
  },
  "devDependencies": {
    "@types/deno": "^2.2.0",
    "@types/mpv-script": "^0.32.8",
    "ts-loader": "^9.5.2",
    "webpack": "^5.99.5",
    "webpack-bundle-analyzer": "^4.10.2",
    "webpack-cli": "^6.0.1"
  }
}
